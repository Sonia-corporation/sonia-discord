import { DECIMAL_COLOR_SILK } from "./decimal-color-silk";

describe(`DECIMAL_COLOR_SILK`, (): void => {
  it(`should be 14082034`, (): void => {
    expect.assertions(1);

    expect(DECIMAL_COLOR_SILK).toStrictEqual(14082034);
  });
});
