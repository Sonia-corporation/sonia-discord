/* eslint-disable @typescript-eslint/no-magic-numbers */

/**
 * @description
 * Add a new version each time the [Firebase DM]{@link IFirebaseDm} model change.
 * Update the [current Firebase DM version]{@link FIREBASE_DM_CURRENT_VERSION} on change.
 */
export enum FirebaseDmVersionEnum {
  V1 = 1,
}
