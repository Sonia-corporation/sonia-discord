/* eslint-disable @typescript-eslint/no-magic-numbers */

/**
 * @description
 * Add a new version each time the [Firebase DM feature]{@link IFirebaseDmFeature} model change.
 * Update the [current Firebase DM feature version]{@link FIREBASE_DM_FEATURE_CURRENT_VERSION} on change.
 */
export enum FirebaseDmFeatureVersionEnum {
  V1 = 1,
}
