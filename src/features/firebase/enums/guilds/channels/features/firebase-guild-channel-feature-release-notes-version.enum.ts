/* eslint-disable no-magic-numbers */

/**
 * @description
 * Add a new version each time the [Firebase guild channel feature release notes]{@link IFirebaseGuildChannelFeatureReleaseNotes} model change
 *
 * Update the [current Firebase guild channel feature release notes version]{@link FIREBASE_GUILD_CHANNEL_FEATURE_RELEASE_NOTES_CURRENT_VERSION} on change
 */
export enum FirebaseGuildChannelFeatureReleaseNotesVersionEnum {
  V1 = 1,
}
