/* eslint-disable no-magic-numbers */

/**
 * @description
 * Add a new version each time the [Firebase guild channel feature]{@link IFirebaseGuildChannelFeature} model change
 *
 * Update the [current Firebase guild channel feature version]{@link FIREBASE_GUILD_CHANNEL_FEATURE_CURRENT_VERSION} on change
 */
export enum FirebaseGuildChannelFeatureVersionEnum {
  V1 = 1,
}
